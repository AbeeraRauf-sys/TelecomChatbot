# Google Gemini (free tier). Get key: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# Optional: Gemini model (default: gemini-2.5-flash-lite for speed). If 404, try gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash-lite
# Optional: RAG embedding model (default: paraphrase-MiniLM-L3-v2 for speed)
# EMBEDDING_MODEL=sentence-transformers/paraphrase-MiniLM-L3-v2

# Optional: override paths (defaults: project_resources/ relative to project root)
# RESOURCES_DIR=project_resources
# STATUS_LOG_PATH=status_logs/actions.log
